# ============================================================
# BundleBox Backend — Python Dependencies
# ============================================================

# --- Core Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==14.2

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0            # PostgreSQL async driver
aiosqlite==0.20.0          # SQLite async (local dev)
alembic==1.14.1            # DB migrations
pymongo==4.8.0             # MongoDB client (optional pipeline result persistence)

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1              # Pin to 4.x for passlib compatibility
httpx==0.28.1              # Async HTTP (Google OAuth token exchange)
email-validator==2.2.0     # Required by Pydantic EmailStr
twilio>=9.0.0              # SMS/WhatsApp OTP

# --- Task Queue ---
celery[redis]==5.4.0
redis==5.2.1
pika==1.3.2

# --- ML / Pipeline ---
opencv-python-headless==4.10.0.84
numpy==2.2.1
Pillow==11.1.0
scipy>=1.12.0
openai==1.59.7
google-genai>=1.70.0

# --- Storage ---
google-cloud-storage==2.19.0

# --- Utilities ---
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
loguru==0.7.3

# --- PyTorch + CLIP (install torch first, then clip) ---
# torch is installed via --extra-index-url in Dockerfile for CUDA support
# clip installed from GitHub (no PyPI release)

# --- Audio Transcription ---
faster-whisper==1.2.1

# --- Dev / Test ---
pytest==8.3.4
pytest-asyncio==0.25.0
